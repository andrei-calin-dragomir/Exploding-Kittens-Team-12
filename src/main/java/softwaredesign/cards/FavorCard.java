package softwaredesign.cards;

import softwaredesign.core.Player;
import softwaredesign.server.ServerHeldGame;

public class FavorCard extends Card {
    public void action(ServerHeldGame heldGame) {
//        String playerWithFavorCard = heldGame.gameManager.getCurrentPlayer().getName();
//        Player target = new Player(); //TODO input here
//        Card receivedCard = new NopeCard(); //TODO received card
//        int indexOfTarget = heldGame.gameManager.getAlivePlayers().indexOf(target);
//        heldGame.gameManager.getCurrentPlayerHand().addToHand(receivedCard);
//        heldGame.gameManager.getAlivePlayers().get(indexOfTarget).getHand().removeCard(receivedCard);
    }
}
